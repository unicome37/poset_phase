# PhaseB 风险预警简报（2048首轮）

生成时间：2026-04-02  
范围：`quick_n2048` 与 `ultra_n2048` 首轮 checkpoint 后的风险评估

## 1) 当前事实

- `quick_n2048.partial.json`：`completed_seed_runs=1/4`
  - seed0: κ=0.3 → rank=2, κ=1.0 → rank=2
- `ultra_n2048.partial.json`：`completed_seed_runs=1/3`
  - seed0: κ=0.3 → rank=2, κ=1.0 → rank=7
- 两路尚未生成最终 `json/md`

## 2) 风险分级

### A. 结论不稳定风险：高

- 在同一 N=2048 下，κ=1.0 的首seed rank 已出现明显分叉：
  - quick: rank=2（看起来稳）
  - ultra: rank=7（强烈异常）
- 这意味着当前结论对采样深度（reps/seed）依赖仍强。

### B. 假阴性/假阳性风险：中高

- 仅凭首seed，极易误判：
  - 可能低估风险（quick 首seed看起来正常）
  - 也可能高估风险（ultra 首seed异常偏大）

### C. 进度延迟风险：中

- 2048 单seed计算成本高，收敛慢；若频繁并行重算，进一步放大延迟。

## 3) 处置建议

1. 维持30分钟低频轮询，先观察 2048 两路从 `1/x` 到 `2/x` 的方向是否一致。  
2. 在 `2/x` 后立即补一份“二点趋势简报”（是否继续发散）。  
3. 主线 `phaseB` 持续运行，作为最终高可信基准，不用当前短样本替代。

## 4) 当前可执行结论（谨慎）

- κ=0.3 在 2048 首seed仍保持 rank=2（与 1024/1536 一致性良好）。
- κ=1.0 在 2048 首seed出现高敏感分叉，暂不宜做单向定论。
